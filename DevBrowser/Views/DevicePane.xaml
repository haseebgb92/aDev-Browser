<UserControl x:Class="DevBrowser.Views.DevicePane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cef="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf">
    <Grid Background="#1A1A1A" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#333333" Padding="10,5" CornerRadius="4,4,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="DeviceLabel" Text="Device Name" FontWeight="Bold" Foreground="White" />
                    <TextBlock x:Name="ResolutionLabel" Text="0 x 0" Foreground="#888888" FontSize="10" />
                    <TextBlock x:Name="ScaleLabel" Text="Scale: 1.0x" Foreground="#888888" FontSize="10" />
                </StackPanel>
                <Button Grid.Column="1" Content="Edit" Padding="10,2" Background="#444444" Foreground="White" BorderThickness="0" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="Black" ClipToBounds="True">
            <Viewbox x:Name="BrowserViewbox" Stretch="Uniform">
                <cef:ChromiumWebBrowser x:Name="Browser" />
            </Viewbox>
        </Border>
    </Grid>
</UserControl>
